[project]
name = "kai-tts"
version = "0.1.0"
description = "Text-to-speech module for K.ai system (part of Kaspar 2028)"
readme = "README.md"
requires-python = "~=3.14.0"
dependencies = [
  "kai-shared",
  "voxcpm>=2.0.2 ; sys_platform == 'linux'",
]

[tool.uv.sources]
kai-shared = { path = "./kai-shared", editable = true }
